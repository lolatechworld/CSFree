# Create a Python program that calculates and 
# outputs the number of characters in a given name using the input function.
print(len(input()))

# Create a Python program that calculates and 
# outputs the number of characters in a list.
character = ["Dami", "Milo", "Momo", "Dady_mo", "2"]
print(len(character))